"""PySide6 GUI for the FH6 skill farm. Run via the root skill_farm_ui.py launcher.

Submodules: theme (stylesheet), widgets (small reusable widgets + log bridge),
farm_tab/settings_tab/timings_tab (one mixin class per tab, combined into
app.SkillFarmWindow).
"""
